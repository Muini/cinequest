<% if object.errors.any? %>
  <p>This form has <%= pluralize(object.errors.count, 'problème') %></p>
  <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>