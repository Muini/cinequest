<h1>CineQuest</h1>
<h2>A la quête d'un nom de film</h2>
<p>Flux CineQuest</p>
<a href="/aventuriers/<%= current_user.pseudo %>">Mon Profil</a><br/>
<a href="/newquete">Nouvelle quète</a><br/>
<a href="/logout">Je pars !</a><br/>
<ul>
<% @posts.each do |post| %>
  <li>
    <p>Proposé par : <%= sanitize(post.user.pseudo) %></p>
    <img src=<%= post.url_img %> alt="L'image de la quète" />
    <% if post.found %>
      <a href=<%= post.url_film %> target="_blank" ><h3><%= sanitize(post.film_name) %></h3></a>
    <% end %>
    <div id="comments-list">
      <%= render partial: post.reload.comments %>
    </div>
  </li>
<% end %>
</ul>