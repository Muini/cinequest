<%= div_for comment, class: (comment.correct ? 'comment_good_answer' : '') do %>
  
    <div class="comment_img">
      <%= image_tag sanitize(comment.user.url_img), alt: "Photo de profil", class: "comment-img" %>
    </div>
    <div class="comment_content">
      <p class="comment-message"><%= sanitize(comment.message) %></p>
      <p class="comment_infos"><%= time_ago_in_words(comment.created_at) %> par <%= link_to sanitize(comment.user.pseudo), user_url(comment.user.pseudo) %></p>
    </div>

    <% if comment.correct %>
        <p class="comment_points_won"><%= sanitize(comment.user.pseudo)+" remporte "+comment.post.difficulty.to_i+1 %> crédits !</p>
    <% end %>

    <% if logged_in? && current_user.id == comment.post.user.id && !comment.post.found && !comment.post.closed %>
        <%= link_to 'Bonne réponse !', post_validate_comment_path(comment.post, comment), method: :get, remote: true, class: "comment_validate" %>
    <% end %>
    
    <% if logged_in? && current_user.id == comment.user.id && !comment.post.found && !comment.post.closed %>
        <%= link_to 'Supprimer', post_comment_path(comment.post, comment), method: :delete, remote: true, class: "comment_delete" %>
    <% end %>

<% end %>