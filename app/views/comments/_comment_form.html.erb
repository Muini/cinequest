<%= form_for([@post, @new_comment], remote: true) do |f| %>

  <%= render 'shared/error_messages', object: f.object %>

  <%= f.text_field :message, placeholder: "Ecrivez votre réponse ici...", class:'lowercase_regular_font blue' %>

  <%= f.submit 'envoyer' %>
<% end %>