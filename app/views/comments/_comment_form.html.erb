<%= form_for([@post, @new_comment], remote: true) do |f| %>

  <%= render 'shared/error_messages', object: f.object %>

  <%= f.text_area :message, placeholder: "Ecrivez votre réponse ici..." %>

  <%= f.submit 'envoyer' %>
<% end %>